# Normalize line endings to LF in the repository; convert to CRLF on checkout for Windows
* text=auto eol=lf

# Force binary treatment for binary assets
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf

# Batch files must stay CRLF on Windows
*.bat text eol=crlf
